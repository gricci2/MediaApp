@page
@model MediaApp.Pages.MediaItems.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>

<form>
    <p>
        <select asp-for="MediaType" asp-items="Model.Types">
            <option value="">All</option>
        </select>
        <label>Title: <input type="text" asp-for="SearchString" /></label>
        <button type="submit" class="btn btn-primary">Filter</button>
        <a asp-page="./Index" class="btn btn-secondary">Reset</a>
    </p>
</form>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MediaItem[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MediaItem[0].Type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MediaItem[0].IsCompleted)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MediaItem[0].Information)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.MediaItem) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsCompleted)
            </td>
            <td>
                    @if (!string.IsNullOrEmpty(item.Information) && item.Information.StartsWith("http"))
                    {
                        <a href="@item.Information" target="_blank">@item.Information</a>
                    }
                    else
                    {
                        @item.Information
                    }
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
